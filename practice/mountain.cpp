#include <bits/stdc++.h>

using namespace std;

int mountain(vector<int> arr)
{
    int mountain = 0;
    int n = arr.size();
    for (int i = 0; i < n - 2;)
    {
    }
}

int main()
{
    vector<int> arr{0, 1, 0};
    cout << mountain(arr) << endl;
}