#include <bits/stdc++.h>

using namespace std;

void rotateArr(int arr[], int d, int n)
{
    // code here
    int temp[n];
    for (int j = 0; j < d; j++)
    {
        temp[j] = arr[j];
    }
    for (int i = 0; i < d; i++)
    {
        arr[i] = arr[i + d];
    }

    for (i =)

        return;
}

int main()
{
    int arr[] = {};
}